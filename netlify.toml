[build]
  base = "public"
  command = "npm run build"
  publish = "public"

[build.environment]
  NODE_VERSION = "20"

# Redirect all requests to index.html for SPA-like behavior
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
